# Upload Instructions

Replace these files on the `Redesign` branch:

```text
README.md
docs/deployment_guide.md
docs/model_card.md
docs/data_dictionary.md
```

No Python scripts, notebook code, model file, dataset, frontend HTML, or CSS need to be changed for this consistency fix.

## GitHub Website Method

1. Open the repository on GitHub.
2. Switch to the `Redesign` branch.
3. Open each file listed above.
4. Click the pencil icon.
5. Replace the old content with the matching file from this package.
6. Commit directly to the `Redesign` branch, or commit to a new branch and open a pull request.

Suggested commit message:

```text
Fix Redesign branch documentation references
```

## What Was Fixed

- Replaced old notebook references from `Final_project_AMIT.ipynb` to `Model_Training_amit.ipynb`.
- Updated the repository structure shown in the README.
- Updated the PythonAnywhere project folder example to use the repository name instead of the branch folder name.
- Kept `app.py`, `final_project_amit.py`, `Model_Training_amit.ipynb`, `model.pickle`, the dataset, and frontend files unchanged.
